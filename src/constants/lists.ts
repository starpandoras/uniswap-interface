// Token lists have been replaced with GraphQL dynamic token fetching
// See src/services/graphql.ts and src/state/tokens/ for the new implementation

// Legacy exports for backward compatibility during migration
export const DEFAULT_TOKEN_LIST_URL = ''
export const DEFAULT_LIST_OF_LISTS: string[] = []
